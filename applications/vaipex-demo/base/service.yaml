apiVersion: v1
kind: Service
metadata:
  name: vaipex-demo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vaipex-demo
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
